<div class="page-header">
    <h1>Bejelentkezés</h1>
</div>

<form class="form-horizontal" method="post">
  <fieldset>
    {{!--<div class="form-group
        {{#if validationErrors.neptun}}
            has-error
        {{/if}}
    ">    --}}
    
      <label for="neptun">Adja meg a használni kívánt neptun kódját!</label>    
      <div class="col-lg-10">
        <input class="form-control" id="neptun" name="neptun" placeholder="pl. ABC123, stb..." type="text" {{!--value="{{data.neptun}}"--}}>
        {{!--{{#if validationErrors.neptun}}
            <span class="help-block">{{validationErrors.neptun.msg}}</span>
        {{/if}}            --}}
      </div>
    {{!--</div>--}}
    
    {{!--
    <div class="form-group
        {{#if validationErrors.password}}
            has-error
        {{/if}}
    ">        --}}
    
      <label for="password">Adja meg a jelszavát:</label>
      <div class="col-lg-10">
        <input class="form-control" id="password" name="password" type="password" {{!--value="{{data.password}}"--}} >
        {{!--
        {{#if validationErrors.password}}
            <span class="help-block">{{validationErrors.password.msg}}</span>
        {{/if}}--}}
      </div>
    {{!--</div>    --}}
    </fieldset>   
    <fieldset class="buttons">    
        <div class="form-group">
            <div class="col-lg-10 col-lg-offset-2">
                <button type="reset" class="btn btn-default" id="Cancel">Cancel</button>
                <button type="submit" class="btn btn-primary Submit" id="Submit">Submit</button>
            </div>
        </div>
   </fieldset>    
</form>
